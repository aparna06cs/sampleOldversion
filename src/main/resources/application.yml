spring:
  application:
    name: configmap-versionsample
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: info,refresh,keepalive,health,env,beans
  security:
    enabled: false