spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: configmap-cart
      reload:
        enabled: true
        mode: event
        strategy: refresh
endpoints:  restart:    enabled: true
