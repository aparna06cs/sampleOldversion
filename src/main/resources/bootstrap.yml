spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: dev2
            name: configmap-config-common-dev2
          - namespace: dev2
            name: configmap-cart
      reload:
        enabled: true
        mode: polling
        strategy: refresh
endpoints:  restart:    enabled: true
