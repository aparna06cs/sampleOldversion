spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        namespace: default
        sources:
          - namespace: dev2
            name: configmap-config-common-dev2
          - name: configmap-cart
      reload:
        enabled: true
        mode: event
        strategy: refresh
endpoints:  restart:    enabled: true
